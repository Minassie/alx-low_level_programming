/**
 * print_numbers - prints all decimal digit
 *
 * Return: values
 */

#include "main.h"

void print_numbers(void)
{
	int a;

	for (a = '0'; a <= '9'; a++)
	{
		_putchar(a);
	}
		_putchar('\n');
}
