#include "main.h"

/**
 * print_most_numbers - prints digits except 2 and 4
 */

void print_most_numbers(void)

{
	int a;

	for (a = 0; a <= 9; a++)
		continue (a = 2 || a =4)
			_putchar(a);
	putchar('\n');
}
